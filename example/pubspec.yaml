name: video_thumbnail_pro_example
description: "Demonstrates how to use the video_thumbnail_pro plugin."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.6

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.1.2
  path_provider: ^2.1.5
  video_thumbnail_pro:
    path: ../
  cupertino_icons: ^1.0.8

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
